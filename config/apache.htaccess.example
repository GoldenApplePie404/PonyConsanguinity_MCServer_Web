# Apache 配置文件
# 将此文件重命名为 .htaccess 并放置在 config/ 目录中

# 禁止直接访问所有 PHP 文件
<FilesMatch "\.php$">
    Order allow,deny
    Deny from all
</FilesMatch>

# 禁止直接访问配置文件
<FilesMatch "config\.php$">
    Order allow,deny
    Deny from all
</FilesMatch>

# 禁止目录列表
Options -Indexes

# 禁止访问所有文件
<RequireAll>
    Deny from all
</RequireAll>
